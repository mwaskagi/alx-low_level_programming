#include "main.h"
#include <stdio.h>

/**
 * main - print the name of program
 * @argc: integer
 * @argv: string array
 * Return: 0
 */

int main(int argc, char *argv[])
{
	int i = 0;

	printf("%s\n", argv[i]);
	return (0);
}
